version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Setting up Node.js v22.16.0"
        - nvm install 22.16.0
        - nvm use 22.16.0
        - export NEXT_TELEMETRY_DISABLED=1
        - export GENERATE_SOURCEMAP=false
        - export NODE_OPTIONS="--max-old-space-size=8192"
        - echo "📦 Installing dependencies using npm ci"
        - npm ci
    build:
      commands:
        - echo "🏗 Running Next.js production build with standalone output"
        - npm run build
        # CRITICAL FIX 1: Copy required-server-files.json to standalone root
        - echo "📋 Copying required-server-files.json to standalone root"
        - cp .next/standalone/.next/required-server-files.json .next/standalone/required-server-files.json
        # CRITICAL FIX 2: Copy trace files to ALL expected locations
        - echo "📋 Copying trace files to all expected locations"
        - cp .next/trace .next/standalone/trace
        - mkdir -p .next/standalone/.next/server
        - cp .next/trace .next/standalone/.next/server/trace
        - cp .next/trace .next/standalone/.next/trace
        - mkdir -p .next/standalone/server
        - cp .next/trace .next/standalone/server/trace
        - echo "✅ All files ready for deployment"
  artifacts:
    baseDirectory: .next/standalone
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
      - ~/.npm