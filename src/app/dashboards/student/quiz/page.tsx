import QuizWrapper from '@/components/layout/main/dashboards/QuizWrapper';

export const metadata = {
  title: 'Quizzes | Student Dashboard',
  description: 'Take course quizzes, track your progress, and review your performance',
};

export default function QuizPage() {
  return <QuizWrapper />;
} 